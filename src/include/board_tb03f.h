/*
 * board_tb03f.h - board selection for the TLSR8258 TB03F module.
 *
 * The GPD has no user GPIO, keyboard, LED, or serial-console service.
 * Keeping this header intentionally empty prevents board-specific application
 * peripherals from being pulled into the firmware.
 */
#pragma once
